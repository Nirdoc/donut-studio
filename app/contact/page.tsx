import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact — Donut Studio",
  description:
    "Contactează-ne pentru comenzi speciale, evenimente sau orice întrebare. Suntem la Piața Victoriei, București.",
};

export default function ContactPage() {
  return <ContactClient />;
}
